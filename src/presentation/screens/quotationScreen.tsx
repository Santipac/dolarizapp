import { Text, View } from 'react-native';

interface QuotationScreenProps {}

export const QuotationScreen = (props: QuotationScreenProps) => {
  return (
    <View>
      <Text>QuotationScreen</Text>
    </View>
  );
};
